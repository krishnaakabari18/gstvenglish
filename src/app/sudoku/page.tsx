import "@/styles/sudokuJS.css";
import SudokuBoard from "@/components/SudokuBoard";

export const metadata = {
  title: "GSTV | Sudoku",
};

export default function SudokuPage() {
  return <SudokuBoard />;
}
